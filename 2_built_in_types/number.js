// @flow

(3.14: number);
(10: number);

// Important: NaN and Infinity are the number type
(NaN: number);
(Infinity: number);

(parseInt("Hi"): number);  // Because NaN is also the number

(new Number(0): Number);

// (0: Number);  // Error
